<h2><%= l(:label_action_new) %> — <%= @risk.title %></h2>

<%= form_with model: [@risk, @action], url: risk_risk_mitigation_actions_path(@risk) do |f| %>
  <%= render 'form', f: f, action: @action %>
  <p><%= f.submit l(:button_create) %></p>
<% end %>
