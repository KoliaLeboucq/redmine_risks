<h2><%= l(:label_action_edit) %> — <%= @risk.title %></h2>

<%= form_with model: [@risk, @action], url: risk_risk_mitigation_action_path(@risk, @action), method: :patch do |f| %>
  <%= render 'form', f: f, action: @action %>
  <p><%= f.submit l(:button_save) %></p>
<% end %>
